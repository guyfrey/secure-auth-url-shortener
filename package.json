{
  "name": "secure-auth-url-shortener",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
"scripts": {
  "start-auth": "cd apps/auth && npm run start",
  "start-shortener": "cd apps/shortener && npm run start"
},
  
  "dependencies": {
    "express-rate-limit": "^8.2.1"
  }
}
